<!DOCTYPE html>
<html>
<head>
  <title>Mood Sounds Recommendation</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #111;
      font-family: Arial, sans-serif;
      color: #ddd;
      text-align: center;
    }
    .container {
      max-width: 1000px;
      margin: 50px auto;
      background-color: #2e2e2e;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.8);
      padding: 30px;
      position: relative;
    }
    h1 {
      margin: 0;
      padding-bottom: 20px;
      font-size: 2.5em;
      color: #fff;
      text-shadow: 0 2px 4px rgba(0,0,0,0.7);
    }
    .info {
      margin-bottom: 20px;
      font-size: 1.2em;
      color: #ccc;
    }
    .sound-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }
    .sound-card {
      background-color: #444;
      border-radius: 8px;
      padding: 15px;
      width: 220px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.7);
      text-align: center;
      transition: transform 0.3s;
    }
    .sound-card:hover {
      transform: scale(1.05);
    }
    .sound-title {
      font-weight: bold;
      color: #fff;
      margin-bottom: 10px;
      text-shadow: 0 1px 2px rgba(0,0,0,0.5);
    }
    .audio-player {
      width: 100%;
      margin-bottom: 10px;
      outline: none;
    }
    .volume-slider {
      width: 80%;
      margin: 0 auto;
    }
    .btn-back {
      position: absolute;
      top: 15px;
      right: 15px;
      background: #5d5df0;
      color: #fff;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      text-decoration: none;
      font-size: 1em;
      cursor: pointer;
      box-shadow: 0 3px 8px rgba(0,0,0,0.6);
      transition: background 0.3s;
    }
    .btn-back:hover {
      background: #4a4ac7;
    }
  </style>
</head>
<body>
  <div class="container">
    <a class="btn-back" href="/dashboard">Back</a>
    <h1>Sounds for <%= chosenMood.toUpperCase() %></h1>
    <div class="info">
      <p>Here are all the .mp3 files we found in the "<%= chosenMood %>" folder. Enjoy!</p>
    </div>
    <div class="sound-grid">
      <% if (soundFiles.length === 0) { %>
        <p>No sounds found in this mood category!</p>
      <% } else { %>
        <% soundFiles.forEach(function(file) { %>
          <div class="sound-card">
            <div class="sound-title"><%= file %></div>
            <!-- Simple audio player -->
            <audio class="audio-player" controls>
              <source src="/music/<%= chosenMood %>/<%= file %>" type="audio/mpeg">
              Your browser does not support the audio element.
            </audio>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</body>
</html>
