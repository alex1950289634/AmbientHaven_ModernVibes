<!DOCTYPE html>
<html>
<head>
  <title><%= title %> - Ambient Haven</title>
  <style>
    /* Dark gradient background, matching your second image style */
    body {
      margin: 0;
      padding: 0;
      background-color: #111;
      font-family: Arial, sans-serif;
      color: #ddd;
    }
    .container {
      max-width: 700px;
      margin: 50px auto;
      background-color: #2e2e2e;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.7);
      padding: 20px;
      text-align: center;
      position: relative;
    }
    h1 {
      margin-top: 0;
      color: #fff;
      text-shadow: 0 1px 2px rgba(0,0,0,0.5);
      font-size: 2em;
    }
    .info {
      margin-bottom: 20px;
    }
    .video-container {
      position: relative;
      margin: 20px auto;
      background-color: #000;
      outline: 1px solid #444;
      box-shadow: 0 2px 8px rgba(0,0,0,0.6);
      width: 90%;
      max-width: 600px;
    }
    video {
      width: 100%;
      display: block;
    }
    .buttons {
      margin-top: 15px;
    }
    /* Purple/blue buttons like your second image */
    .back-btn {
      padding: 10px 15px;
      border: none;
      border-radius: 4px;
      background-color: #5d5df0;
      color: #fff;
      cursor: pointer;
      margin: 0 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.5);
      font-size: 1em;
      text-decoration: none;
      display: inline-block;
      transition: background 0.3s;
    }
    .back-btn:hover {
      background-color: #4a4ac7;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Now Playing</h1>
    <div class="info">
      <p>You selected <strong><%= title %></strong>.</p>
      <p>Enjoy the sound in the center of the page!</p>
    </div>
    <div class="video-container">
      <!-- We loop so the MP4 repeats; "controls" for user play/pause -->
      <video controls loop>
        <source src="/<%= filePath %>" type="video/mp4">
        Your browser does not support the video tag.
      </video>
    </div>
    <div class="buttons">
      <a class="back-btn" href="/dashboard">Back to Dashboard</a>
    </div>
  </div>
</body>
</html>
