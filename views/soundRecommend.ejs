<!DOCTYPE html>
<html>
<head>
  <title>Get Sounds Recommendation</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #1c1c1c;
      font-family: Arial, sans-serif;
      color: #ddd;
    }
    .container {
      max-width: 600px;
      margin: 80px auto;
      background-color: #2e2e2e;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.7);
      padding: 20px;
      text-align: center;
    }
    h1 {
      margin-top: 0;
      color: #fff;
      text-shadow: 0 1px 2px rgba(0,0,0,0.5);
    }
    .desc {
      margin-bottom: 20px;
      line-height: 1.5em;
      color: #ccc;
    }
    select {
      width: 60%;
      padding: 8px;
      font-size: 1em;
      border-radius: 4px;
      border: none;
      background-color: #444;
      color: #eee;
      margin-bottom: 20px;
    }
    button {
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      background-color: #5d5df0;
      color: #fff;
      font-size: 1em;
      cursor: pointer;
      box-shadow: 0 2px 5px rgba(0,0,0,0.5);
    }
    button:hover {
      background-color: #4a4ac7;
    }
    a {
      color: #66aaff;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Select Your Mood</h1>
    <p class="desc">
      What is your mood now? Let us help you find the perfect sound that resonates with how you feel.
    </p>
    <form action="/soundrecommend" method="POST">
      <select name="category" required>
        <option value="">-- Select Your Mood --</option>
        <% categories.forEach(function(cat) { %>
          <option value="<%= cat %>"><%= cat %></option>
        <% }); %>
      </select>
      <br>
      <button type="submit">Recommend Me</button>
    </form>
    <p style="margin-top:20px;">
      <a href="/dashboard">Back to Dashboard</a>
    </p>
  </div>
</body>
</html>
